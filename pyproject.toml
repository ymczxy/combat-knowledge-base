[project]
name = "combat-knowledge-base"
version = "1.6.0"
description = "Unified combat knowledge base and Godot data pipeline"
requires-python = ">=3.10"

[project.scripts]
ckb = "ckb.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[project.optional-dependencies]
docs = ["mkdocs-material>=9.5,<10"]
